# ! DO NOT COMMIT YOUR LocalConfig.cmake FILE !

# Use this file as an example to link your OpenGL common libraries

# Exemple common path, change it to your own path
# If on windows, you can use ${USER_HOME_DIRECTORY} instead of $ENV{HOME}
add_subdirectory(${USER_HOME_DIRECTORY}/Repositories/OpenGL/common ${CMAKE_BINARY_DIR}/common_build)
target_link_libraries(${PROJECT_NAME} PRIVATE common)
